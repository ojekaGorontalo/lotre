// ==================================================== // WINGO AUTO-BOT v4.2 - PAUSE SETELAH 3x LOSS, RESUME SETELAH 3x WIN // DIMODIFIKASI: Metode prediksi 4 angka + 3 mode strategi // VERIFIKASI UID TETAP (1999645) + MODAL UI TOAST // ====================================================  (function() {     'use strict';      // ============================================================     // 0. FUNGSI MODAL / TOAST (UI NOTIFICATION)     // ============================================================     function showToast(message, type) {         // Hapus toast lama jika ada         const oldToast = document.getElementById('wingoToast');         if (oldToast) oldToast.remove();          const toast = document.createElement('div');         toast.id = 'wingoToast';         let bgColor = '#1e293b';         if (type === 'success') bgColor = '#22c55e';         else if (type === 'error') bgColor = '#ef4444';         else if (type === 'info') bgColor = '#3b82f6';          toast.style.c...� Bot direset. Mode kembali ke 1 (PERTAMBAHAN).`);         showToast('🔄 Bot reset! Level 1, Mode 1 (PERTAMBAHAN)', 'success');     }      // ============================================================     // 11. INIT DAN EKSPOS OBJEK     // ============================================================     hookApi();      window.wingoAuto = {         start: startBot,         stop: stopBot,         status: status,         reset: resetBot     };      console.log(`✅ WINGO AUTO-BOT v4.2 (UID tetap + Modal) siap!`);     console.log(`📌 Perintah: wingoAuto.start() / stop() / status() / reset()`);     console.log(`⚙️ Mode strategi: 1=PERTAMBAHAN, 2=REVERSE, 3=ZIGZAG (otomatis berubah saat kalah)`);     showToast('✅ Bot siap! Ketik wingoAuto.start()', 'success');      // ============================================================     // 12. LANGSUNG JALANKAN BOT SETELAH VERIFIKASI SUKSES     // ============================================================     window.wingoAuto.start();  })();
